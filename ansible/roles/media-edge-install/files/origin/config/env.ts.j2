const hosts = JSON.parse('{{ dns_config.domains | to_json }}');
module.exports = {
  caddyUrl: "http://127.0.0.1:2020/",
  elasticSearchUrl: "http://127.0.0.1:9200",
  MARKETPLACE_ID: {{marketplace_id}},
  debug: true,
  hosts: hosts,

  dbName: "{{db_name}}",
  dbUser: "{{db_user}}",
  dbPassword: "{{db_password}}",
  dbHost: "{{db_host}}",
  dbPort: "{{db_port}}",
  dbDialect: "postgres",

  cname: "{{cname}}",
  a_record: "{{origin_ip}}"

  providers: {{wallets | to_json}}
}