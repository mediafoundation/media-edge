import { Env } from "./interfaces";

export const env: Env = {
    MARKETPLACE_ID: 1,
    dbName: "{{db_name}}",
    dbUser: "{{db_user}}",
    dbPassword: "{{db_password}}",
    dbHost: "postgres-origin",
    dbPort: 5432,
    dbDialect: "postgres",
    caddyUrl: "http://caddy-origin:2020/",
    elasticSearchUrl: "http://es01:9200",
    debug: true,
    providers: {{providers | to_json}},
}