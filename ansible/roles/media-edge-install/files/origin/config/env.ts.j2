const hosts = JSON.parse('{{ dns_config.domains | to_json }}');

interface Provider {
    supportedChains: number[]
    accountIndex: number
    privateKey?: string[]
    mnemonic?: string
}

export interface Env {
    MARKETPLACE_ID: number;
    PURGE_PASSWORD: string;
    dbName: string,
    dbUser: string,
    dbPassword: string,
    dbHost: string,
    dbPort: number,
    dbDialect: "postgres",
    caddyUrl: string;
    hosts: string[];
    hns: string;
    cname: string;
    a_record: string;
    providers: Provider[];
    debug?: boolean
    elasticSearchUrl: string
}

export const env: Env = {
    MARKETPLACE_ID: 1,
    PURGE_PASSWORD: "",
    dbName: "{{db_name}}",
    dbUser: "{{db_user}}",
    dbPassword: "{{db_password}}",
    dbHost: "{{db_host}}",
    dbPort: {{db_port}},
    dbDialect: "postgres",
    caddyUrl: "http://127.0.0.1:2020/",
    hns: "", // dev
    cname: "{{cname}}",
    a_record: "{{origin_ip}}",
    hosts: hosts,
    elasticSearchUrl: "http://127.0.0.1:9200",

    providers: [
        {
            mnemonic: "asd",
            supportedChains: [1, 2, 3],
            accountIndex: 0
        },

        {
            mnemonic: "s",
            supportedChains: [1, 2, 3],
            accountIndex: 1
        }
    ]
}