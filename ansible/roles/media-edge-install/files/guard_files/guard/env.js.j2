module.exports = {
    testing: {{is_testing|lower}},
    rpc: "{{solana_rpc}}",
    programId: "{{programId}}",
    host: "{{dns_config.domain}}",
    hns: "{{domain_hns}}",
    dbstring: "postgres://guard:{{postgresPassword}}@localhost:5432/guard",
    decrypt_publicKey: "{{decrypt_publicKey}}",
    decrypt_privateKey: "{{decrypt_privateKey}}",
    minimum_amount: {{minimum_amount}},
    caddyUrl: "{{caddyUrl}}",
    cloudns_api_url: "{{dns_config.api_url}}",
    cloudns_sub_auth_id: "{{dns_config.user}}",
    cloudns_auth_pass: "{{dns_config.password}}"
  }
  